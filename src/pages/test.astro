<img src="/bertie.jpg" />

<style lang="scss">
	img {
		position: fixed;
		top: 0px;
		left: 0px;
		width: 100%;
		height: 100%;
		object-fit: contain;
		--x: 6rem;
		--y: 0px;
		clip-path: circle(3rem at var(--x) var(--y));

		transition:
			scale 0.25s ease-out 0s,
			opacity 0.25s ease-out 0.25s;
		transition-property: scale, opacity;

		scale: var(--scale);
		/*transform: translate(
		calc(var(--x) / var(--scale)),
		calc(var(--y) / var(--scale))
	);*/
		transform-origin: var(--x) var(--y);

		pointer-events: none;

		// -1px for border
		clip-path: circle(0.5rem at var(--x) var(--y));

		z-index: 11;
	}
</style>
